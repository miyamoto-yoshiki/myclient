# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore uploaded files in development.
/storage/*
!/storage/.keep

/public/assets
.byebug_history

# Ignore master key for decrypting credentials and more.
/config/master.key

/public/packs
/public/packs-test
/node_modules
/yarn-error.log
yarn-debug.log*
.yarn-integrity

.Ds_Store
# 実際にコミットを行う前に、不要な変更履歴がGitで管理されないようにしましょう。
# ここでいう不要な変更履歴とは、.DS_Storeという隠しファイルのことです。このファイルはディレクトリの情報を記録するファイルです。度々生成されるものですが、このファイルをGitで管理することによって、今後必要のない作業が増えてしまう可能性があります。ここで設定を行って、今後は.DS_StoreをGitで管理しないようにしましょう。